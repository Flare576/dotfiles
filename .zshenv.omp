# ~/.zshenv.omp — OMP shell utilities
# Sourced from ~/.zshenv (symlinked from ~/dotfiles/.zshenv.omp)
#
# Start dev-mode OMP in the current dir
function domp() {
  curdir="$(pwd)"
  pushd ~/Projects/Personal/oh-my-pi && bun dev --cwd "$curdir" && popd
}
